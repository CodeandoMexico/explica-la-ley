<h2 class="col-md-offset-2">Actualizar ley</h2>
<form method="POST" action="edit" class="col-md-4 col-md-offset-2 pure-form pure-form-stacked" role="form">
  <fieldset>
    <label for="name">Nombre</label>
    <input type="text" class="form-control" id="name" name="name" value="<%= law.name %>" placeholder="Nombre" required />
    <br />

    <label for="summary">Resumen (unas 20 palabras)</label>
    <input type="text" class="form-control" id="summary" name="summary" placeholder="Resumen" value="<%= law.summary %>" required />
    <br />

    <label for="summary">Slug</label>
    <input type="text" class="form-control" id="slug" name="slug" placeholder="nombre-de-ley" value="<%= law.slug %>" required />
    <small>SLUG: Debe de contener un nombre único y URL-friendly; de preferencia sin la palabra "Ley" (por ejemplo: federal-de-impuestos).</small>
    <br />
    <br />

    <label for="tag">Tag/Reforma a la que pertenece:</label>
    <select name="tag">
      <option selected value="">-LEY INDEPENDIENTE-</option>
      <% _.each(tags, function(tag) {%>
        <% if (law.tag == tag.id) { %>
          <option selected value="<%= tag.id %>"><%= tag.name %></option>
        <% } else { %>
          <option value="<%= tag.id %>"><%= tag.name %></option>
        <% } %>
      <% }) %>
    </select>
    <br />

    <input type="hidden" id="id" name="id" value="<%= law.id %>" />
    <button type="submit" class="pure-button el-button el-button-primary">Actualizar</button>
  </fieldset>
</form>
