<h2 class="col-md-offset-2">Actualizar tag</h2>
<form method="POST" action="edit" class="col-md-4 col-md-offset-2 pure-form pure-form-stacked" role="form">
  <fieldset>
    <label for="name">Nombre</label>
    <input type="text" class="form-control" id="name" name="name" value="<%= tag.name %>" placeholder="Nombre" required />
    <br />

    <label for="name">Resumen corto (unas 20 palabras)</label>
    <input type="text" class="form-control" id="summary" name="summary" value="<%= tag.summary %>" placeholder="Resumen" required />
    <br />

    <label for="slug">Slug</label>
    <input type="text" class="form-control" id="slug" name="slug" value="<%= tag.slug %>" placeholder="nombre-de-reforma" />
    <small>SLUG: Debe de contener un nombre único y URL-friendly; de preferencia sin la palabra "Reforma" (por ejemplo: telecomunicaciones-y-transporte).</small>
    <br />
    <br />

    <input type="hidden" id="id" name="id" value="<%= tag.id %>" />
    <button type="submit" class="pure-button el-button el-button-primary">Actualizar</button>
  </fieldset>
</form>
